interface:
  display_name: "Knowlaborator My Day"
  short_description: "Compose the labeled cross-organization morning brief"
  default_prompt: "Use $knowlaborator-my-day to enumerate my authorized organizations, read Today in each one, restore my prior active organization, and compose one labeled read-only brief without acting."
